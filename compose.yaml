services:
  excel-ai-analysis:
    build:
      context: .
      dockerfile: Dockerfile
    image: excel-ai-analysis:local
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
